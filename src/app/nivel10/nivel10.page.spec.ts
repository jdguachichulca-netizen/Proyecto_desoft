import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel10Page } from './nivel10.page';

describe('Nivel10Page', () => {
  let component: Nivel10Page;
  let fixture: ComponentFixture<Nivel10Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel10Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
