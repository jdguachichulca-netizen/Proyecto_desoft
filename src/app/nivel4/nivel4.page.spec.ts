import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel4Page } from './nivel4.page';

describe('Nivel4Page', () => {
  let component: Nivel4Page;
  let fixture: ComponentFixture<Nivel4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
