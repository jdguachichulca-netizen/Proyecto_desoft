import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel16Page } from './nivel16.page';

describe('Nivel16Page', () => {
  let component: Nivel16Page;
  let fixture: ComponentFixture<Nivel16Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel16Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
