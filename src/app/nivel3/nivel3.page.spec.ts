import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel3Page } from './nivel3.page';

describe('Nivel3Page', () => {
  let component: Nivel3Page;
  let fixture: ComponentFixture<Nivel3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
