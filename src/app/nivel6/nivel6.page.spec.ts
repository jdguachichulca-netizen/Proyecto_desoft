import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel6Page } from './nivel6.page';

describe('Nivel6Page', () => {
  let component: Nivel6Page;
  let fixture: ComponentFixture<Nivel6Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel6Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
