import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel7Page } from './nivel7.page';

describe('Nivel7Page', () => {
  let component: Nivel7Page;
  let fixture: ComponentFixture<Nivel7Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel7Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
