import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel11Page } from './nivel11.page';

describe('Nivel11Page', () => {
  let component: Nivel11Page;
  let fixture: ComponentFixture<Nivel11Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel11Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
