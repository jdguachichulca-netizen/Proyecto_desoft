import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel15Page } from './nivel15.page';

describe('Nivel15Page', () => {
  let component: Nivel15Page;
  let fixture: ComponentFixture<Nivel15Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel15Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
