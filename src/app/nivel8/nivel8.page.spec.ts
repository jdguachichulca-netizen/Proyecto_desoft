import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel8Page } from './nivel8.page';

describe('Nivel8Page', () => {
  let component: Nivel8Page;
  let fixture: ComponentFixture<Nivel8Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel8Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
