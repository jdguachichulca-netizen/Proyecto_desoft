import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel9Page } from './nivel9.page';

describe('Nivel9Page', () => {
  let component: Nivel9Page;
  let fixture: ComponentFixture<Nivel9Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel9Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
