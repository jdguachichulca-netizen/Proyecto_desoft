import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel12Page } from './nivel12.page';

describe('Nivel12Page', () => {
  let component: Nivel12Page;
  let fixture: ComponentFixture<Nivel12Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel12Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
