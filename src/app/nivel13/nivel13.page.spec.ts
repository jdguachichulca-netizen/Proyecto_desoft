import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel13Page } from './nivel13.page';

describe('Nivel13Page', () => {
  let component: Nivel13Page;
  let fixture: ComponentFixture<Nivel13Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel13Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
