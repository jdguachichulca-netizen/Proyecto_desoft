import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel14Page } from './nivel14.page';

describe('Nivel14Page', () => {
  let component: Nivel14Page;
  let fixture: ComponentFixture<Nivel14Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel14Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
