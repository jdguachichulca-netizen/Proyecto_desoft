import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Nivel5Page } from './nivel5.page';

describe('Nivel5Page', () => {
  let component: Nivel5Page;
  let fixture: ComponentFixture<Nivel5Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Nivel5Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
